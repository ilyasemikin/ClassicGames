#pragma once

#include <string>

namespace Settings {
    const size_t WindowWidth = 300;
    const size_t WindowHeight = 600;
    const std::string WindowTitle = "Snake";
    
    const float Latency = 0.1;

    const size_t FieldWidth = 16;
    const size_t FieldHeight = 30;
}